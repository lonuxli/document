# Linux命令之常用命令

删除linux下缓存文件，避免跳转打开再次提示

find \-name \*.swp |  xargs rm

批量文件重命名

rename 's/^aarch64\-linux\-gnu/aarch64\-linux\-5\\.5/' aarch64\-linux\-gnu

target remote localhost:1234

b start\_kernel

通过ssh 进行夸服务器间的文件拷贝

scp lilong@192.168.119.153:/share/gitwork/mycode/pack\_shop.tar.gz ./
